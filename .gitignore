# Built binaries live at the kit root.
trainer_win_x64.exe
trainer_linux_x64
trainer_colab
BUILD_MANIFEST.txt
.build/

# Corpora and weights. The filipino corpus alone is 2.9 GB and the 1M-game
# target is roughly 5.5 GB.
data/
*.ntc
*.ntw
*.bin
*.log
nnue_weights.h
